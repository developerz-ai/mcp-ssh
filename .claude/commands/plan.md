Explore the codebase for the task below before writing any code. Read the relevant modules (start from the `Where to look` table in CLAUDE.md), trace the real flow end to end, then write a short implementation plan: the files you'll touch, the change in each, and one verification per step (`step → verify: check`). Surface assumptions and call out any simpler approach. Do not implement yet.

Task: $ARGUMENTS
